# Week 9 — Secrets, Security & the Full Pipeline

**Estimated time:** 60–90 minutes
**Goal:** Understand every secret in your project, learn what happens if one leaks, and see the entire pipeline as a complete system.

---

## What you will learn

- What GitHub Secrets are and how they work under the hood
- The three secrets in your project and what each one does
- What to do if a secret is leaked
- How to read the full pipeline end-to-end

---

## Theory (20 min)

### GitHub Secrets explained

GitHub Secrets are **encrypted environment variables** stored in GitHub's vault. When a workflow runs:

1. GitHub decrypts the secret inside the Actions runner (an isolated VM)
2. The secret is injected as an environment variable
3. GitHub **masks** the value in logs — if your code accidentally prints it, GitHub replaces the output with `***`
4. The secret is never accessible to PR workflows from forked repositories (security protection against PRs from strangers exfiltrating your keys)

**Secrets are not magic** — they are just encrypted strings. Their security depends on:
- Keeping them out of your code (never hardcode an API key)
- Rotating them regularly
- Using the principle of least privilege (give a key only the permissions it needs)

---

### The secrets in your project

| Secret | What it is | What happens if it leaks |
|--------|-----------|--------------------------|
| `GITHUB_TOKEN` | Auto-generated by GitHub per workflow run | Expires after the run — low risk |
| `AZURE_FOUNDRY_RESOURCE` | The name of your Azure AI Foundry resource | Attacker knows *where* to call — but still needs the key |
| `AZURE_FOUNDRY_API_KEY` | API key for your Claude deployment on Azure | Attacker can call Claude at your expense — rotate immediately |

### What to do if a secret leaks

If you accidentally commit an API key to a public repo:
1. **Assume it is compromised** — GitHub's secret scanning bots and malicious actors scrape new commits in seconds
2. **Rotate it immediately** — go to [ai.azure.com](https://ai.azure.com) → your project **Home** page → regenerate the **Project API key**
3. **Update the GitHub Secret** with the new value
4. **Remove it from git history** — `git filter-repo` or GitHub's "Remove secret" tool
5. Check Azure **Cost Management** for unexpected charges

---

## Tasks

### 1. Audit your secrets

Go to **Settings → Secrets and variables → Actions** in your GitHub repository.

You should see:
- `AZURE_FOUNDRY_RESOURCE`
- `AZURE_FOUNDRY_API_KEY`

`GITHUB_TOKEN` is not listed there — it is automatically available in every workflow as `${{ secrets.GITHUB_TOKEN }}`.

For each secret, answer:
- What service does it belong to?
- What can an attacker do with it?
- Where would I go to rotate it?

Write the answers in a comment in the PR you open for this week (see Task 4).

### 2. Test secret masking in logs

Add a temporary step to any workflow, push, and look at the logs:

```yaml
- name: Test secret masking
  run: echo "The key is ${{ secrets.AZURE_FOUNDRY_API_KEY }}"
```

In the logs you will see:
```
The key is ***
```

Remove this step after testing — never intentionally log secrets.

### 3. Draw the full pipeline

On paper or in a diagram tool (even a comment in your PR), draw this pipeline and fill in every component:

```
Developer writes code
        ↓
git push (feature branch)
        ↓
GitHub receives the push
        ↓
[GitHub Actions - CI]
  ↓ htmlhint validates index.html
  ↓ PASS ✅ / FAIL ❌
        ↓
[Cloudflare Pages - Preview]
  ↓ Builds preview deployment
  ↓ Posts preview URL on PR
        ↓
[GitHub Actions - AI Review]
  ↓ Gets PR diff
  ↓ Calls Claude API (Azure AI Foundry)
  ↓ Posts review comment on PR
        ↓
Developer reviews feedback → fixes issues → pushes again
        ↓
All checks pass ✅
        ↓
PR merged to main
        ↓
[Cloudflare Pages - Production]
  ↓ Builds production deployment
  ↓ Live at https://your-project.pages.dev
```

For each box, note:
- What triggers it?
- What does it produce?
- What secret (if any) does it use?

### 4. Create a final PR that demonstrates the full pipeline

```bash
git checkout -b feature/week-9-final
```

Make one final improvement to `index.html` — add a footer, update the description, whatever you like.

```bash
git add index.html
git commit -m "Final improvements to landing page"
git push -u origin feature/week-9-final
```

Open a Pull Request. Watch the full pipeline run:
- ✅ HTML lint check
- 🔗 Cloudflare preview URL
- 🤖 Claude AI review comment

Merge the PR. Visit your production URL.

### 5. Reflect on what you have built

After merging, answer these questions (as a comment or in a notes file):

1. What is the difference between CI and CD?
2. Why do we use feature branches instead of committing to `main`?
3. What are two things that could go wrong in a CI/CD pipeline and how would you debug them?
4. If your team had 5 developers, what would you add to this pipeline?

---

## Commit your work

Your final `index.html` change was committed on `feature/week-9-final` and merged into `main` via PR in Task 4. After merging:

```bash
git checkout main
git pull
git status
```

Expected output:
```
On branch main
nothing to commit, working tree clean
```

Final state of your repository on `main`:

```
DevOps/
├── .github/
│   └── workflows/
│       ├── ci.yml          ← Week 5
│       ├── claude.yml      ← Week 7
│       └── ai-review.yml   ← Week 8
├── .gitignore              ← Week 2
├── .htmlhintrc             ← template (verified Week 5)
├── LICENSE
├── README.md
├── course/
│   └── week-01.md … week-09.md
├── index.html              ← customized (Week 2+)
├── script.js
└── style.css               ← customized (Week 3+)
```

---

## Expected result

- [ ] All secrets audited and understood
- [ ] Pipeline drawn and labeled
- [ ] Final PR demonstrates the complete automated pipeline
- [ ] Reflection questions answered

---

## Congratulations — you completed the course

Here is what you built over 9 weeks:

| Week | What you learned | What you built |
|------|-----------------|----------------|
| 1 | GitHub, fork, clone | Working local environment |
| 2 | Git loop (add/commit/push) | First commit on GitHub |
| 3 | Branches & Pull Requests | Feature branch workflow |
| 4 | Cloudflare Pages | Live site at a public URL |
| 5 | GitHub Actions | Automated HTML validation |
| 6 | Branch protection & previews | Protected main, PR previews |
| 7 | AI in CI/CD | Claude reviewing every PR |
| 8 | Minimal AI code review in Foundry | Baseline model call in CI |
| 9 | Secrets & security | Full pipeline understood |

**Your pipeline:** `git push → CI lint → AI review → CF preview → merge → CF production deploy`

This is real DevOps. The same principles (automated checks, branch protection, secrets management, continuous deployment) apply at companies of any size — just with more complexity.

---

## What to explore next

- **GitHub Environments** — separate staging and production deployments with manual approval gates
- **Lighthouse CI** — automated performance, accessibility, and SEO scoring
- **Dependabot** — automated dependency update PRs
- **CODEOWNERS** — require specific reviewers for specific files
- **Terraform / Pulumi** — manage cloud infrastructure as code

---

> You are no longer a DevOps beginner.
